apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
#  - ingress.yaml
# default image (will be overridden by overlays/CI)
images:
  - name: account-service
    newTag: 0.0.1
