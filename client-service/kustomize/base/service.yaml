apiVersion: v1
kind: Service
metadata:
  name: client-service
  namespace: services
  labels:
    app: client-service
spec:
  selector:
    app: client-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP  # internal-only, exposed via Ingress